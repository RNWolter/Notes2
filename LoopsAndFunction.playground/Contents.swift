//: Playground - noun: a place where people can play

import Cocoa

function lambdaSchool(factor1: Int, factor2 Int limit: Int  ){
    
    
    for number in 0...limit{
        if number % factor1 == 0 && number % factor2 
    }
    
}
